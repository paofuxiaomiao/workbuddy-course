# WorkBuddy ABCD 截图复刻

主入口：原项目首页 `/`。ABCD 已融入原导航、首页与功能指南；`/practice` 保留为独立截图对照入口。

## 运行

```sh
npx --yes pnpm@10.4.1 install --frozen-lockfile
npm run dev -- --host 127.0.0.1 --port 5176
```

浏览器打开 http://127.0.0.1:5176/ 。左侧“课程实操”与首页入口可进入 A—D；专家·技能·连接器、资料库、项目导航分别进入对应练习。功能指南新增“读完就试一试”入口，原项目/专家概览可从实操顶部“功能概览”打开。

整合版共用原侧栏，实操组件持续挂载，切回首页或其他模块后已启用技能、导入资料、项目配置及单元进度保留；刷新页面清空教学状态。右下角可查看各单元 1—4 步、重新练习和打开原图对照。实操进度与原探索进度分别记录，不把点击入口算作完成。

## 还原方法

- React 19 + TypeScript，沿用项目的 Vite、Wouter、Lucide。
- 独立对照页以截图约 1650 × 900 的逻辑尺寸等比缩放；整合版只嵌入内容区，使用原项目侧栏和可用高度，窄窗口允许横向滚动。
- 首页、侧栏任务列表、技能市场、专家卡片和详情、资料库双侧栏、项目配置和邀请面板均用 DOM/CSS 绘制。
- 头像、会议图标、机器人和案例缩略图使用用户原始 PNG 的 CSS 局部取景；可操作界面并非整张截图铺底。
- 原图保存在 `client/public/practice/reference/`。新组件样式均在 `.rep-stage` / `.rep-host` 范围内，不覆盖原课程样式。
- 字体由系统渲染，和原截图可能存在抗锯齿差异。未截图的后续输入、结果和练习导航为补充模拟界面。

## 四项流程

| 单元 | 路径 |
| --- | --- |
| A | 技能市场 → 腾讯会议 → 开启技能 → 去试试 → 发送预约任务 → 填写会议主题/日期/时间 → 查看并核对模拟会议 |
| B | 专家市场 → 小红书运营专家 → 召唤 → 发送任务 → 回答受众等问题 → 提供原始材料 → 生成文案、调整语气并核对 |
| C | 资料库 → 新建“小红书表格” → 快速添加/上传和导入 → 点文件 → 引用到任务 → 阅读 → 打开可视化页面 |
| D | 项目 → 进入培训项目 → 邀请/模拟成员加入 → 右侧分别添加专家和技能 → 选择云端任务 → 输入并发送协作任务 |

D 的模型选择由发起者控制；切换成员视角后模型下拉框禁用，并提示积分由发起者承担。

## 模拟边界

本版本为本地教学仿真，没有接入腾讯会议、模型 API、WorkBuddy 资料库或多人后端，不发送真实邀请、不消耗积分。文案由固定模板结合输入材料组成。资料库示例使用教学数据，虽展示截图中的 `.xlsx` 文件名，未伪造实际工作簿内容；本地文件选择读取 TXT/CSV/Markdown 文本，存于页面内存，刷新清空。生成的页面是本地 React 预览，不推送到 WorkBuddy。

## 验证

`npm run check` 和 `GITHUB_PAGES=true npm run build` 用于类型与生产构建检查。构建有原项目动态/静态混合导入和大包体提示，不阻止完成。浏览器检查覆盖主界面、技能/专家弹窗、资料库菜单及各核心任务路径；整合检查覆盖原首页导航、无重复侧栏、功能指南直达、原功能概览往返、页面切换保留资料和技能，以及知识库完整流程完成后同步侧栏进度。

源码在 main 分支维护，GitHub Pages 从 gh-pages 分支发布构建产物。发布目录额外包含 practice/index.html，支持实操页直接访问。阅读素材入口保留原 PDF 下载及 16 页预览。
